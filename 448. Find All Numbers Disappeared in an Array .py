class Solution:
    def findDisappearedNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        index = 0
        for i in nums:
            if nums[index] = index 
               nums[index] = -1
            for j in nums:
                if nums != -1:
                    ans.append()
                
        
        # list_a = set(i for i in range(1, len(nums)+1))
        # nums = set(nums)
        # ans = list_a.difference(nums)
        # return list(ans)
    
    
        # list_a = [i for i in range(1, len(nums)+1)]
        # print(list_a)
        # ans = []
        # for i in list_a:
        #     if i not in nums:
        #         ans.append(i)
        # return ans  